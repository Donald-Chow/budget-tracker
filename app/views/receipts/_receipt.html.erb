<div id="<%= dom_id receipt %>">

  <div class="mx-auto w-full flex p-4">
    <div class="w-full">
      <h1 class="font-bold text-4xl"><%= receipt.store %></h1>
      <div class="flex justify-between">
        <h1 class="font-bold text-4xl"><%= receipt.date.strftime('%Y-%m-%d') %></h1>
        <h1 class="font-bold text-4xl"><%= number_to_currency(receipt.items.sum(:amount), unit: '¥', precision: 0) %></h1>
      </div>
      <div class="mt-4">
        <%= render 'items/items', items: receipt.items %>
      </div>
    </div>
  </div>

</div>
